---
import { Image } from "astro:assets";
const logos = await Astro.glob("../../public/images/companies logo/*.webp");
---

{
  logos.map((logo) => {
    return (
      <div
        class="fade flex items-center will-change-transform justify-center h-[170px] flex-card_team_sm sm:flex-card_full hover:shadow-card_shadow hover:shadow-black/40 duration-300 ease-in-out rounded group"
        data-animation="animate-fade animate-duration-[2000ms]"
      >
        <Image
          src={logo.default}
          alt=""
          loading="lazy"
          class="w-28 h-auto grayscale group-hover:grayscale-0 duration-300 ease-in-out object-contain"
        />
      </div>
    );
  })
}
