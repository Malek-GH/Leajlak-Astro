---
import type { ImageMetadata } from "astro";
import { getImage } from "astro:assets";
interface Props {
  link: string | ImageMetadata;
}
const { link } = Astro.props;

const Img = await getImage({
  src: link,
  format: "webp",
});
---

<link rel="preload" href={Img.src} as="image" />
